


<%#= link_to("cancel", "/bookings/#{@booking.id}", {method: :delete} ) %>
<%= link_to "< Home", root_path %>

<h1>Confirmation</h1>

<div>
  <br>
  <!-- "#{@musician.find(@booking.customer_id).full_name}" -->

    <p>Thank you for requesting a booking with <%= @booking.musician.full_name %>!</p>
      <br>
    <p>Below are your booking details:</p>
      <br>
      <br>
    <p>Booking Request for: <%= @booking.musician.full_name %></p>
    <p>Ensemble type: <%= @booking.musician.ensemble_type %> </p>
    <p>Instruments: <%= @booking.musician.instruments %></p>
    <p>Repertoire: <%= @booking.repertoire_name %> | <%= @booking.repertoire_length %> minutes </p>
      <br>
      <br>
    <p>Date and time: <%= @booking.date_time.strftime('%F at: %H:%M sharp!') %></p>
    <p>Address: <%= @booking.location %></p>
    <p>Comments: <%= @booking.comments %></p>
    <p>Price: $<%= User.find(@booking.musician_id).price %></p>
    <p>Booking status: Pending...</p>

</div>

<div>
  <p>Once <%= @booking.musician.full_name %> accept/s your booking,
  you will be notified by email on <%= current_user.email %>.
  In the meantime, you can reach <%= @booking.musician.full_name %> by phone on <%= @booking.musician.phone_number %>
  if you have additional questions or comments.</p>
</div>


<div>
  <p><%= link_to "OK", bookings_path %></p>
</div>

<!-- <div>
  <p> <%= link_to "CANCEL BOOKING", booking_path(@booking), method: :delete %> </p>
 </div> -->

<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
  <div>
    "I MADE THIS BOOKING BY MISTAKE. CANCEL BOOKING"
    <!-- Launch demo modal -->
  </div>
</button>

<!-- Modal -->
<div class="modal fade centered-modal" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel"></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body modal-centered-element">
        <strong>CANCELLATION SUCCESSFUL</strong>
        <br>
        <br>
        Your booking request has been <br>
        successfully cancelled.
        <div>
          <br>
          <%= link_to "OK", booking_path(@booking), method: :delete, class: "btn btn-primary-ok" %>
        </div>
      </div>









